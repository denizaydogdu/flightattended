# flightattended
flightattended

## Description
config-server, server.port=8888

discovery-service, server.port=8761

api-gateway, server.port=8080

flight-service, server.port=8081

## Build and start
./build-all.sh

docker-compose up -d